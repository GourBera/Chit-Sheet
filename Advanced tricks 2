import big_o


def min_coin(value):
    coins = [25, 10, 5, 1]
    dct = dict(value=value)
    for coin in coins:
        if value < coin:
            continue
        else:
            count = value // coin
            dct[coin] = count
            value -= coin * count

    print(dct)


# Complexity
best, other = big_o.big_o(min_coin, big_o.datagen.n_, max_n=2)
print(best)
print(other)

